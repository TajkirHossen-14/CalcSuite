# Banner

Drop banner / hero images for CalcSuite in this folder.

Reference them from your markup with a root-relative path, for example:

```html
<img src="Assets/Banner/hero.png" alt="CalcSuite banner">
```

> This placeholder file only exists so the folder is tracked by Git — Git does not
> store empty directories. Delete it once you have added your own files.
